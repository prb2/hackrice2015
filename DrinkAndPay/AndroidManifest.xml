<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hackrice.team.drinkup"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="hackrice.team.drinkup.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EventPage"
            android:label="EventPage" />
        <activity
            android:name=".DetailPage"
            android:label="DetailPage" />
        <activity
            android:name=".VenmoWebViewActivity"
            android:label="VenmoWebViewActivity" />
        <activity
            android:name=".PaymentPage"
            android:label="PaymentPage" />
        <activity
            android:name=".PaymentPage"
            android:label="PaymentPage" />


        <meta-data android:value="@string/facebook_app_id" android:name="com.facebook.sdk.ApplicationId"/>
    </application>

</manifest>
